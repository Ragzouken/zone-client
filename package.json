{
  "name": "zone-client",
  "version": "1.0.0",
  "description": "a client for zones",
  "main": "index.js",
  "scripts": {
    "build-script": "browserify src/main.ts -p [ tsify --noImplicitAny ] -o dist/script.js -s zone",
    "build-pug": "pug --pretty src/index.pug -o dist",
    "build": "npm run build-script && npm run build-pug",
    "prestart": "npm run build",
    "start": "light-server-pug -o -s dist -w './src/** # npm run build'",
    "test": "jest --config jestconfig.json",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "publish": "npm run build && neocities upload -d zone ./dist/*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ragzouken/zone-client.git"
  },
  "keywords": [
    "zone"
  ],
  "author": "mark wonnacott",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Ragzouken/zone-client/issues"
  },
  "homepage": "https://github.com/Ragzouken/zone-client#readme",
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "browserify-shim": "^3.8.14",
    "jest": "^25.3.0",
    "prettier": "^2.0.4",
    "ts-jest": "^25.4.0",
    "tsify": "^4.0.1",
    "tslint": "^6.1.1",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "blitsy": "^0.2.1"
  }
}

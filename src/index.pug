doctype html
html
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=600")
        title zone
        style
            include style.css
        script
            include library/blitsy.js
        script
            include script.js
    body
        #scene
            #insert
                #noyoutube 
                    span youtube has not loaded
                #youtube
            canvas#scene-canvas(width=512, height=512)
        canvas#chat-canvas(width=512, height=512)
        #chat-inputs
            input#chat-name(type="text", placeholder="your name...")
            input#chat-input(type="text", placeholder="chat message...")
        #entry-splash
            #entry-panel
                h1 you are about to enter a zone
                input#join-name(type="text", placeholder="your name...")
                button#entry-button(disabled) enter
